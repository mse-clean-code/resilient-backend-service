# This test file can be used in the future to substitute the tmdb api through wiremock or another service
# Use test class with annotation `@ActiveProfiles("wiremock")`
tmdb.api.uri=http://localhost:9000
tmdb.image.uri=http://localhost:9000